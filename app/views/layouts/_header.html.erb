<header class="header">

  <nav class="nav-header group">
    <a href="<%= root_url %>" class="header-name">Shelf Life</a>
    <ul class="nav-list">
			<!-- <ul class="profile-nav"> -->
    		<li><a class="explore" href="/#explore">Explore</a></li>

      <% if signed_in? %>
			<li> <a class="header-profile" href="/#users/<%= current_user.id %>"><%= current_user.name.capitalize %></a></li>
      <li><form action="<%= session_url %>" method="post" class="group">
        <%= auth_token %>

        <input name="_method" type="hidden" value="delete">
        <button class="log-out">Sign Out</button>
      </form></li>
      <% else%>

      <!-- <form action="<%= new_user_url %>" method="get" class="group">
        <%= auth_token %>
        <button class="log-in">Sign Up</button>
      </form> -->
      <li><form action="<%= new_session_url %>" method="get" class="group">
        <%= auth_token %>
        <button class="log-in">Sign In</button>
      </form></li>
      <% end %>
    <!-- </ul> -->
	</ul>
  </nav>
</header>
